# Configuration of the camera
camera:
{
    resolution = { width = 1920; height = 1080; };
    position = { x = -100; y = 0; z = 20; };
    rotation = { x = 0.0; y = 0; z = 0.0; };
    fieldOfView = 72.0; # In degree
};

# Primitives in the scene
primitives:
{
    # List of spheres
    spheres = (
        {
            x = 5; y = 60; z = 40; r = 25;
            material = {
                name = "Mirror";
                color = { r = 255; g = 64; b = 64; }
            };
        }
        ,
        {
            x = 20; y = -40; z = -10; r = 35;
            material = {
                name = "Flat color";
                color = { r = 64; g = 255; b = 64; }
            };
        }
    );
    planes = (
        {
            axis = "Z"; position = -20;
            material = {
                name = "Mirror";
                color = {  r = 64; g = 64; b = 255; }
            };
        }
    );

    pyramids= (
    {
        origin = { x = 5; y = 40; z = 0; }; h = 40.0; w = 40.0; l = 40.0;
        material = {
            name = "Flat color"
            color = { r = 128; g = 255; b = 2; };
        };
    }
    );
};

# Light configuration
lights:
{
    ambient = 0.4; # Multiplier of ambient light
    diffuse = 0.6; # Multiplier of diffuse light

    # List of point lights
    point_lights = (
        { x = 100.0; y = 400.0; z = 500.0; color = { r = 255; g = 255; b = 255; }; falloutDistance = 10000.0; }
    );

    # List of directional lights
    directional = ();
};